# Copy to the deployment-local secret companion only when required.
# Values are write-only operational secrets and must never be committed.
# PLEX_TOKEN=<value>
# HOME_ASSISTANT_TOKEN=<value>
# HOME_ASSISTANT_EVENT_TOKEN=<value>
# Calendar whole-URL secrets and per-user audiobook credentials are added only
# when their corresponding canonical fields are enabled.
